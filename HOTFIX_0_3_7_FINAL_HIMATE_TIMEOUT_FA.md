# Hotfix 0.3.8 - HiMate + Timeout Final

این نسخه ترکیبی است و همه اصلاحات قبلی را هم‌زمان دارد:

- برند نمایشی برنامه: HiMate Sync
- حذف عبارت نمایشی «حضور» از UI مشتری
- افزایش تایم‌اوت ارتباط با سرور
- مخفی‌کردن خطای خام HTTPSConnectionPool از UI مشتری
- پشتیبانی از دستورهای سرور: ENROLL_FINGER / DELETE_FINGER / SET_TIME

## نکته مهم
اگر داخل برنامه هنوز `read timeout=4` یا عبارت «حضور» می‌بینی، یعنی Setup جدید نصب نشده و هنوز EXE قدیمی در حال اجراست.

بعد از Commit در GitHub، Release جدید را دانلود و Setup را نصب کن.
